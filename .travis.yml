sudo: required
os: windows
group: dev
language: bash

cache:
  branch: ha-win-debug
#  debug: true
  directories:
    - test
script:
  - curl -o C:/Users/travis/.casher/bin/casher https://raw.githubusercontent.com/travis-ci/casher/ha-win-debug/bin/casher
  - curl -O https://raw.githubusercontent.com/travis-ci/casher/ha-win-debug/bin/casher && mv casher C:/Users/travis/bin/casher
  - casher
